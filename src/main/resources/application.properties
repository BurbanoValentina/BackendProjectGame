# MySQL Database Configuration
spring.datasource.url=${DATABASE_URL:jdbc:mysql://up-de-fra1-mysql-1.db.run-on-seenode.com:11550/db_1pyy7warrpf2}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=${DATABASE_USERNAME:db_1pyy7warrpf2}
spring.datasource.password=${DATABASE_PASSWORD:W2K2v4xTxm5M1g2zomF0OhHF}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Server configuration for SeeNode deployment
server.port=${PORT}
server.address=0.0.0.0

# Backend URL for CORS and frontend communication
backend.url=${BACKEND_URL}
