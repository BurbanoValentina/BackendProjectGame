## Configuración por defecto (producción/desarrollo) usando SQLite
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.datasource.username=Tinaynn
spring.datasource.password=Tinaynn1401

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect

# Server configuration (usa PORT si existe, si no 8080)
server.port=${PORT:8080}
server.address=0.0.0.0

# CORS frontend URL (lee de env FRONTEND_URL o usa default)
app.frontend.url=${FRONTEND_URL:http://localhost:5173}

# Ruta del archivo SQLite (puede sobrescribirse con la variable de entorno DB_PATH)
spring.datasource.url=jdbc:sqlite:${DB_PATH:data/events.db}
## MongoDB (opcional): establece esta URI para habilitar conexión a Mongo Atlas
# Reemplaza <db_password> por tu contraseña real o usa variable de entorno MONGO_URI
spring.data.mongodb.uri=${MONGO_URI:mongodb+srv://Tinaynn:Tinaynn1401@cluster0.ixdbzsm.mongodb.net/?appName=Cluster0}
# Nombre de base de datos a usar en MongoDB (no debe contener espacios)
spring.data.mongodb.database=${MONGO_DATABASE:eventsdb}

# Variables de entorno opcionales (defínelas en tu entorno del sistema operativo)
# DB_PATH=c:\ruta\otra.db
# MONGO_URI=mongodb+srv://usuario:pass@cluster/...
# MONGO_DATABASE=eventsdb
